---
import {type IProjectExperiences} from "@types";
import ScrollAnimationWrapper from '@components/common/ScrollAnimationWrapper.astro';
const {data} = Astro.props as IProjectExperiences;
---
{
    data.map((project) => (
    <div class="w-full" style="margin: 0 50px 0 50px;">
        <ScrollAnimationWrapper>
            <div class="my-8">
                <div class="flex justify-between items-center flex-wrap">
                    <h3 class="text-xl font-semibold">{project.name}</h3>
                </div>
                <p class="text-sm mt-3 text-location-blue">{project.location}</p>
                <ul class="mt-3">
                    {project.descriptions.map((item) => (
                            <li class="dark:text-background-color">{item}</li>
                    ))}
                </ul>
            </div>
        </ScrollAnimationWrapper>
    </div>
        ))
    }
